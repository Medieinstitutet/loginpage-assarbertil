{
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel --no-source-maps --dist-dir public",
    "build": "parcel build --no-source-maps --dist-dir public",
    "build-no-server": "parcel build --no-source-maps --dist-dir public --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.0.1"
  }
}
